{
  "name": "ict-daily-quiz",
  "private": true,
  "scripts": {
    "build": "python -m pip install --disable-pip-version-check pymupdf pillow && python scripts/process_incoming.py && mkdir -p site && cp styles.css site/styles.css && cp quiz-data.json site/quiz-data.json && rm -rf site/daily_assets && cp -R daily_assets site/daily_assets && sed -i \"s|const MANIFEST_URL = .*|const MANIFEST_URL = '/quiz-data.json';|\" site/app.js && sed -i \"s|const ASSET_BASE = .*|const ASSET_BASE = '/daily_assets';|\" site/app.js",
    "start": "echo static-site"
  },
  "dependencies": {}
}
